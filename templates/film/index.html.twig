{% extends 'base.html.twig' %}

{% block title %}Hello FilmController!{% endblock %}

{#{% form_theme leFormulaire 'bootstrap_5_layout.html.twig' %}#}

{% block body %}
    {% for film in films %}
        <hr>
        <h4>{{ film.nom }}</h4>
        <h4>{{ film.resume }}</h4>
        <h4>{{ film.realisateur }}</h4>
{#        <h4> {{ film.released_at}} </h4>#}

        <br>
        <a href="{{ path('unfilm', { id:film.id}) }}" class="btn btn-primary">Voir ce film</a>


    {% endfor %}
{% endblock %}
